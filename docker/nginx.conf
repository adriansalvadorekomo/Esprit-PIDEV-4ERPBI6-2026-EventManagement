server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Required when accessed via ngrok
    add_header ngrok-skip-browser-warning "true" always;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
